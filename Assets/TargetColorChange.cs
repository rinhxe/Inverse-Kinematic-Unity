using UnityEngine;

public class TargetColorChange: MonoBehaviour {
    //[SerializeField] private Material thisSMaterial;
    //Material tempMaterial;
    //[SerializeField] private MeshRenderer meshRenderer;
    //[SerializeField] private Transform target;

    //private void Start() {
    //    tempMaterial = new Material;
    //    meshRenderer.material = tempMaterial;
    //}

    //private void Update() {
    //    float distance = Vector3.Distance(transform.position, target.position);

    //    float gValue = Mathf.Clamp01(distance / 10f) * 255f;
    //    float bValue = gValue;

    //    tempMaterial.SetColor("_Color", new Color(tempMaterial.color.r, gValue, bValue, tempMaterial.color.a));
    //}
}
